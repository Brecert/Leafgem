module Leafgem
  class Draggable
    def dragging?
      # use `Hitbox#in_bounds?`
    end

    def clicked?
      # use `Hitbox#in_bounds?`
    end

    def hovering?
      # use `Hitbox#in_bounds?`
    end
  end
end
